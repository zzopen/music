#!/usr/bin/env bash

git add . && git commit -m 'fix:暂存' && git push origin main
# GIT_BIN=$(which git)
# $("${GIT_BIN} add . " && \ "${GIT_BIN} commit -m 'fix: 暂存'" && \ "${GIT_BIN} push origin main")