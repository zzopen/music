import { defineStore } from 'pinia'
import { router } from '@/routers'

export const useRouteStore = defineStore({
	id: 'app-route',
	state: () => ({
		
		
	}),
	getters: {
		
	},
	actions: {
		
	}
})
